# G-1-5 学習ループ

| # | タスク                         | 備考                                                                                                                 | 完了 |
| - | --------------------------- | ------------------------------------------------------------------------------------------------------------------ | -- |
| A | ModelConfig に vocab_size 追加 | —                                                                                                                  | ✅ |
| B | LanguageModel::forward      | embedding → n_layers TransformerBlock → final_norm → lm_head → logits                                              | ✅ |
| C | CrossEntropyLoss（GPU）       | logits → loss scalar + ∂loss/∂logits（fused softmax, log-sum-exp）                                                   | ✅ |
| D | backward カーネル（WGSL）         | matmul_backward / rms_norm_backward / swiglu_elementwise_backward / rope_backward / embedding_backward / attention_backward | ✅ |
| E | LanguageModel::backward     | B の逆順で D のカーネルを呼ぶ                                                                                                  | ✅ |
| F | AdamW optimizer             | CPU版 adam_w.rs を移植                                                                                     | ✅ |
| G | 学習ループ                       | forward → loss → backward → AdamW → checkpoint 保存                                                                  | — |
| H | データローダー                | ファイルからテキストをロード                                                                                          | ✅ |
| I | チェックポイント                | CPU版 checkpoint.rs を移植                                                                                          | ✅ |
| J | スケジューラ                | CPU版 lr_scheduler.rs を移植                                                                                          | ✅ |
| K | ログの拡充                | ema, bpc 追加                                                                                          | - |

[D] TODO: rms_norm_backward の dgamma が CPU 計算となっており、未最適化 

## [A] ModelConfig に vocab_size 追加

`vocab_size` / `n_kv_heads` / `dropout_p` を追加

```rust
pub struct ModelConfig {
    pub vocab_size: usize,
    pub d_model: usize,
    pub n_heads: usize,
    pub n_kv_heads: usize,
    pub d_ff: usize,
    pub n_layers: usize,
    pub max_seq_len: usize,
    pub dropout_p: f32,
    pub eps: f32,
    pub rope_base: f32,
}
```

テスト用にデフォルト設定を追加

```rust
impl Default for ModelConfig {
    fn default() -> Self {
        Self {
            vocab_size: 256, // テスト用デフォルト
            d_model: 64,
            n_heads: 4,
            n_kv_heads: 4,
            d_ff: 128,
            n_layers: 2,
            max_seq_len: 128,
            dropout_p: 0.1,
            eps: 1e-6,
            rope_base: 10000.0,
        }
    }
}
```

テストで変更が必要なパラメータのみ設定。

```rust
let cfg = ModelConfig {
    n_heads: 1,
    ..Default::default()
};
```

## [B] LanguageModel::forward
### Embedding の実装

`embedding.wgsl` を追加

```wgsl
let i = gid.x;
let token_id = token_ids[i];
let d = dims.d_model;

for(var j: u32 = 0u; j < d; j++) {
    out[i * d + j] = weight[token_id * d + j];
}
```

テストを実施

```
running 1 test
test kernel::embedding::test::test_embedding ... ok
```

### LanguageModel::forward の実装

既に作成済みのカーネルを組み合わせて完了

* embedding → n_layers TransformerBlock → final_norm → lm_head → logits

学習用パラメータ (`embedding`、`final_gamma`、`lm_head`) は LanguageModel が管理する。


`test_utils.rs` に `random_token_ids` を追加。len 個の token id を生成し、テストを実施

```
running 1 test
max_rel_err = 1.84e-2  [PASS]
test model::language_model::test::test_language_model ... ok
```

## [C] Cross Entropy loss（GPU）

* CPU側は softmax を明示的に計算してから log を取る
* GPU側は probs を全部保存できないため、`max_val` と `sum_exp` のスカラー2個だけ持てば良い `log-sum-exp` で Cross Entropy Loss を算出

CPU側をGPU側の計算方法に合わせても良い。

```
CPU版: logits → probs (保存) → loss, probs → grad
1. Softmax
2. Loss
3. dlogits[i,j] = probs[i] - 1(i == target)

GPU版: logits → lse (スカラー) → loss と grad を同一ループで計算
1. max_val = max_j logits[i,j]
2. sum_exp = Σ_j exp(logits[i,j] - max_val)
3. lse = max_val + log(sum_exp)
4. loss_i = lse - logits[i, target_i]
5. dlogits[i,j] = exp(logits[i,j] - lse) - 1{j=target_i}
```

`cross_entropy_loss.wgsl` を追加し、テスト実施

```bash
running 3 tests
max_rel_err = 8.28e-8  [PASS]
max_rel_err = 8.28e-8  [PASS]
max_rel_err = 8.28e-8  [PASS]
test kernel::cross_entropy_loss::test::test_cross_entropy_known_value ... ok
test kernel::cross_entropy_loss::test::test_loss_is_mean_over_seq ... ok
test kernel::cross_entropy_loss::test::test_target_boundary ... ok
```

## [D] backward カーネル（WGSL）

逆伝搬の追加

- 各層の forward cache struct を入れる ✅ 完了
  LanguageModelForwardCache に token_ids, x0, block_inputs, block_outputs, final_norm_in, final_norm_out, logits を持たせる
- matmul_backward ✅ 完了
- rms_norm_backward ✅ 完了
- swiglu_elementwise_backward ✅ 完了
- rope_backward ✅ 完了
- embedding_backward ✅ 完了
- attention_backward ✅ 完了

cross_entropy_loss の backward は cross_entropy_loss のカーネルで grad も同時に計算しているため不要。

### matmul_backward
`matmul.wgsl` に転置フラグを追加

* test_trans_a ・・・ x^T @ W のテスト
* test_trans_b ・・・ x @ W^T のテスト
* test_trans_ab ・・・ x^T @ W^T のテスト

```bash
running 8 tests
max_rel_err = 2.03e-5  [PASS]
max_rel_err = 2.99e-6  [PASS]
test kernel::matmul::tests::test_matmul_non_tile_boundary ... ok
test kernel::matmul::tests::test_trans_ab ... ok
max_rel_err = 0.00e0  [PASS]
max_rel_err = 1.73e-7  [PASS]
test kernel::matmul::tests::test_matmul_identity ... ok
test kernel::matmul::tests::test_trans_b ... ok
max_rel_err = 1.06e-6  [PASS]
test kernel::matmul::tests::test_matmul_forward ... ok
max_rel_err = 2.68e-4  [PASS]
test kernel::matmul::tests::test_matmul_random ... ok
max_rel_err = 9.35e-7  [PASS]
test kernel::matmul::tests::test_trans_a ... ok
max_rel_err = 5.02e-6  [PASS]
max_rel_err = 2.00e-5  [PASS]
test kernel::matmul::tests::test_matmul_backward ... ok
```

### rms_norm_backward

`dx`, `dgamma` の計算が必要だが、Metal 環境の制約により `dgamma` は CPU で計算

```
// Too many bindings of type StorageBuffers in Stage ShaderStages(COMPUTE), limit is 4, count was 5.
```

`test_rms_norm_backward` / `test_rms_norm_backward_random` を追加

```bash
running 4 tests
max_rel_err = 1.76e-7  [PASS]
test kernel::rms_norm::test::test_rms_norm_random ... ok
max_rel_err = 9.17e-6  [PASS]
max_rel_err = 1.06e-5  [PASS]
test kernel::rms_norm::test::test_rms_norm ... ok
test kernel::rms_norm::test::test_rms_norm_backward ... ok
test kernel::rms_norm::test::test_rms_norm_backward_random ... ok
```

### swiglu_elementwise_backward

Metal 環境の制約があるため、`d_gate` と `d_up` を連結した `d_out` として扱う。

```bash
running 2 tests
max_rel_err = 1.35e-7  [PASS]
test kernel::swiglu_elementwise::test::test_swiglu_elementwise ... ok
test kernel::swiglu_elementwise::test::test_swiglu_elementwise_backward ... ok
```

### rope_backward

`rope_backward.wgsl` 追加

```bash
running 5 tests
max_rel_err = 3.23e-7  [PASS]
test kernel::rope::test::test_rope_backward ... ok
test kernel::rope::test::test_position_zero ... ok
test kernel::rope::test::test_rope_random ... ok
max_rel_err = 1.24e-6  [PASS]
test kernel::rope::test::test_rope ... ok
test kernel::rope::test::test_rope_backward_random ... ok
```

### embedding_backward

`embedding_backward.wgsl` 追加

```
running 2 tests
test kernel::embedding::test::test_embedding_backward ... ok
test kernel::embedding::test::test_embedding ... ok
```

### flash_attenstion_backward

`flash_attention_backward.wgsl` 追加

```bash
running 7 tests
max_rel_err = 2.59e-5  [PASS]
max_rel_err = 2.59e-5  [PASS]
max_rel_err = 1.58e-5  [PASS]
max_rel_err = 2.58e-5  [PASS]
max_rel_err = 2.58e-5  [PASS]
max_rel_err = 1.57e-5  [PASS]
max_rel_err = 1.13e-7  [PASS]
max_rel_err = 1.13e-7  [PASS]
max_rel_err = 7.99e-8  [PASS]
test kernel::attention::test::test_casual_backward_mask ... ok
max_rel_err = 0.00e0  [PASS]
max_rel_err = 0.00e0  [PASS]
max_rel_err = 0.00e0  [PASS]
max_rel_err = 6.48e-8  [PASS]
max_rel_err = 6.48e-8  [PASS]
test kernel::attention::test::test_softmax_row_sum ... ok
test kernel::attention::test::test_backward_row ... ok
max_rel_err = 2.62e-5  [PASS]
max_rel_err = 1.66e-7  [PASS]
max_rel_err = 0.00e0  [PASS]
max_rel_err = 0.00e0  [PASS]
test kernel::attention::test::test_attention_non_power_of_two ... ok
test kernel::attention::test::test_casual_mask ... ok
max_rel_err = 2.30e-1  [PASS]
max_rel_err = 1.66e-2  [PASS]
max_rel_err = 2.43e-3  [PASS]
test kernel::attention::test::test_attention_backward_random ... ok
max_rel_err = 1.73e-2  [PASS]
max_rel_err = 2.48e-7  [PASS]
test kernel::attention::test::test_attention_random ... ok

test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 38 filtered out; finished in 2.62s
```

## [B] LanguageModel::backward
- ffn backward ✅ 完了
- attention backward ✅ 完了
- transformer_block backward ✅ 完了
- langate_model backward ✅ 完了

## [F] AdamW optimizer
 - AdamWParam / AdamW ✅ 完了

## [G] 学習ループ（forward → loss → backward → SGD/Adam）
- `train.rs` ✅ 完了

## [H] データローダー
- `dataset.rs` ✅ 完了

`from_file` / `from_str` でトークナイズ後、`sample_window` で `seq_len` 取り出す。

- `Dataset::from_file`: ファイルからトークナイズ
- `Dataset::from_str`: 文字列からトークナイズ
- `Dataset::sample_window`: ランダムウィンドウを1つ取り出す

## [I] チェックポイント
- `checkpoint.rs` ✅ 完了

### チェックポイントに保存する情報
- 再開するポイントのメタ情報: step, best_val
- モデルのコンフィグ: vocab_size, d_model, n_heads, n_kv_heads, d_ff, n_layers, max_seq_len
- モデル: embedding, final_gamma, lm_head, blocks 内の weight 
- オプティマイザ: m, k, data

`start_step=0` から開始

```bash
# d_model=64, n_heads=4, n_kv_heads=4, d_ff=128, n_layers=2, max_seq_len=128, vocab_size=256, dropout=0.1
# train_tokens=892315, val_tokens=223079, start_step=1, best_val=inf
step,loss,ms_per_step,elapsed_s
100,7.9714,314.8,31.5
200,7.3142,316.0,63.1
300,6.1617,297.8,92.9
400,5.5964,285.7,121.4
500,4.6783,296.1,151.0
# step 500 val_loss=4.9428 ppl=140.16
# → checkpoint saved (val_loss=4.9428)
600,4.4500,377.4,188.8
700,4.2187,321.0,220.9
```

`start_step=500` から再開

```bash
# resume from checkpoint: checkpoints/best.ckpt
# d_model=64, n_heads=4, n_kv_heads=4, d_ff=128, n_layers=2, max_seq_len=128, vocab_size=256, dropout=0.1
# train_tokens=892315, val_tokens=223079, start_step=501, best_val=4.9427733
step,loss,ms_per_step,elapsed_s
600,4.1212,305.3,30.5
700,3.5926,295.5,60.1
800,3.9783,289.0,89.0
900,3.3003,301.1,119.1
1000,3.7107,292.2,148.3
# step 1000 val_loss=3.4175 ppl=30.49
# → checkpoint saved (val_loss=3.4175)
1100,3.0839,330.6,181.4
```

## [J] スケジューラ
- `lr_scheduler.rs` ✅ 完了

### Warmup -> Stable

```bash
# d_model=64, n_heads=4, n_kv_heads=4, d_ff=128, n_layers=2, max_seq_len=128, vocab_size=256, dropout=0.1
# lr_max=0.001, lr_min=0.0001, warmup_steps=200, end_step=5000, lr_schedule=WarmupStableDecay { stable_steps: 3800 }, start_step=1, best_val=inf
# train_tokens=892315, val_tokens=223079
step,loss,ms_per_step,elapsed_s
100,8.5828,5.000e-4,322.3,32.2
200,8.1072,1.000e-3,330.7,65.3
300,6.6729,1.000e-3,315.8,96.9
400,6.5626,1.000e-3,307.7,127.6
```

### Stable -> Decay

```bash
3600,3.9970,1.000e-3,338.2,1169.3
3700,3.9092,1.000e-3,305.5,1199.8
3800,3.6098,1.000e-3,321.3,1232.0
3900,3.8544,1.000e-3,327.0,1264.7
4000,3.5368,1.000e-3,314.4,1296.1
# step 4000 val_loss=3.7549 ppl=42.73
4100,3.8398,7.154e-4,330.9,1329.2
4200,3.5557,5.975e-4,308.4,1360.0
4300,3.9365,5.070e-4,333.0,1393.4
4400,4.4117,4.308e-4,313.6,1424.7
4500,3.5868,3.636e-4,284.4,1453.1
# step 4500 val_loss=3.7443 ppl=42.28
4600,3.6995,3.029e-4,321.5,1485.3
4700,3.4983,2.470e-4,297.8,1515.1
4800,4.1487,1.950e-4,321.3,1547.2
4900,3.7492,1.462e-4,292.7,1576.5
5000,4.1361,1.000e-4,278.1,1604.3
# step 5000 val_loss=3.8216 ppl=45.68
```

## [K] ログの拡充

- train データに対して `ema` , `ppl` , `ema_ppl` を追加
- val データに対して、`bpc` を追加

`ema` , `ppl` , `bpc` は以下の通り
- ema は、データのばらつきを平滑化した補助指標
- ppl は、次のトークンをどれだけ「迷っているか」を表す評価指標
- bpc (bits per token) は 1 文字あたりの cross-entropy

```bash
step,loss,ema,ppl,ema_ppl,lr,ms_per_step,elapsed_s
100,8.5827,8.5607,5338.6670,5222.3926,5.000e-4,326.9,32.7
200,8.3072,7.9269,4052.8230,2770.7761,1.000e-3,328.3,65.5
300,6.8909,7.0657,983.2828,1171.1467,1.000e-3,311.5,96.7
400,6.2139,6.3532,499.6537,574.3342,1.000e-3,318.1,128.5
500,5.1053,5.5418,164.8867,255.1368,1.000e-3,294.2,157.9
# val step=500 val_loss=5.4268 val_ppl=227.4283 bpc=7.8293
# → checkpoint saved (val_loss=5.4268)
```